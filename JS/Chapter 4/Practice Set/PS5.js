/*JavaScript strings are immutable — once a string is created, its characters cannot be modified individually.
Even if you try something like this:*/
let name ="Utkarsh"
name[3]="C";
console.log(name)
